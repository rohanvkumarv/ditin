import React from "react";

const page = () => {
    return (
        <div className="h-[90%] text-xl font-mono flex items-center justify-center">
            Cart is Empty
        </div>
    );
};

export default page;
